const Footer = () => (
    <footer className="bg-gray-800 text-white py-6 text-center">
        <p>&copy; {new Date().getFullYear()} ByteCraft.dev — Wszystkie prawa zastrzeżone.</p>
    </footer>
);

export default Footer;
